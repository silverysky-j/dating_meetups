
  # Порция знакомств

  Лендинг проекта «Порция знакомств» — бранчи и ужины для тех, кто открыт к знакомствам.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
